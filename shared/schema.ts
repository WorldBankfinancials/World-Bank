/**
 * shared/schema.ts
 *
 * Re-exports from packages/shared/schema.ts so that drizzle.config.ts
 * and any other tooling referencing this path get the full schema.
 */

export * from '../packages/shared/schema';
