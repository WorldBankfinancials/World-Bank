# Compliance Domain

This domain handles regulatory compliance, including policy enforcement, regulatory reporting, and compliance rule management for the banking platform.

## Structure
- `application/` - Use cases, commands, queries, handlers
- `domain/` - Entities, aggregates, repositories, services, events
- `infrastructure/` - Database persistence, cache, messaging, external adapters
- `interfaces/` - REST, GraphQL, gRPC, WebSocket, events
